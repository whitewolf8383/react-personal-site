import React from 'react';

// Components
import HeaderTitle from './HeaderTitle';

function Header() {
  return(
    <header id="home">
      <HeaderTitle />
    </header>
  );
}

export default Header;